import * as THREE from "three";

export const GRAVITY = 0.02;
export const JUMP_FORCE = 0.3;
export const GROUND_LEVEL = 1; // Assuming 0 is the ground level
export const DEFAULT_CAMERA_OFFSET = new THREE.Vector3(0, 0.5, 2);
export const MAX_CAMERA_MULTIPLIER = 2;
export const MIN_CAMERA_MULTIPLIER = 0.5;